<div class="ui-page">
  <div class="ui-page-header">
    <div>
      <p class="text-xs font-black uppercase tracking-wide text-subtle">Users</p>
      <h1 class="font-display text-3xl font-black text-ink">ユーザー新規作成</h1>
    </div>
  </div>

  <%= render Ui::CardComponent.new(padded: true) do %>
    <%= render "form", user: @user %>
  <% end %>
</div>
